
Pastebin service

Usage:

  GET  /:id       Retrieve a paste
  POST /          Create a new paste
  POST /:id       Create or overwrite a paste
  DELETE /:id     Delete a paste

Example:

  cat hello.txt | curl example.com/hello

  curl example.com/hello -F file=@hello.txt


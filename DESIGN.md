# Pastebin

一个简单的 Pastebin web 服务端

## 目标

用法保持简短，方便在命令行使用。paste 无所有权概念，任何人可读、可写、可删，除非被锁定

## 杂项

- 所有响应体都是纯文本格式，Content-Type始终是text/plain，请求体默认是纯文本格式，兼容 multipart/from-data 格式，使用file字段
- 限制请求体大小，使用配置项单条 paste 大小上限 + multipart 容错偏移量
- 不用对 paste 进行文本解码，以原始字节进行存储和传输
- paste 的访问请求允许客户端，代理服务器缓存，在适当时返回 304，只使用更新时间等元数据生成相关头，不计算内容哈希
- 管理员使用 basic 认证，管理员忽略速率限制
- id 生成使用随机字符串，通过配置文件定义长度、字符、碰撞检测次数，碰撞次数耗尽则长度+1重
新生成并在之后使用该长度，如果长度+1后超过长度限制，则拒绝写入
- 根据 id 长度，设定ttl，由配置文件定义，更新后用基于更新时间重新设置过期时间
- 锁定的paste不能被覆盖，不会过期，实现是把过期时间设置为字段的理论最大值，解锁时基于当前时间重新设置过期时间
- 用量是指paste大小的累计，覆盖更新时应减去旧的大小，累计用量是仅用于实时速率限制窗口
- 速率限制使用固定时间窗口限制，通过配置文件定义，允许多个窗口，基于ip对请求频次、流量(应用层)、用量进行限制，读写分开限制，默认不限制读取
- 数据库大小超过限制时拒绝写入，使用定时器检查数据库大小
- 使用定时器删除过期过期的条目
- 在特定时间自动统计生成并缓存报告，报告当前数据库大小，paste 数量，最新的10条id和时间(updated_at)，内存占用情况，报告时间等


## API

- `GET /` 响应 ./help.txt (内存缓存)
- `GET /:id` 响应paste内容，不存在或已过期返回404
- `POST /:id` 保存paste，响应访问url，如果被锁定，需要管理员
- `POST /`  等同于 `POST /生成id`
- `DELETE /:id` 删除 paste ，如果被锁定，需要管理员
- `PUT /:id?action=lock`  锁定一个条目，需要管理员
- `PUT /:id?action=unlock`  解锁一个条目，需要管理员
- `GET /?action=stats` 响应缓存的统计信息，如果是管理员，立即触发统计更新缓存后再响应

## 数据库

时间相关字段使用毫秒时间戳表示

- pastes 表
 - id string 
 - content blob
 - size int
 - locked bool
 - created_at long 
 - updated_at long
 - expired_at long 
 - created_by string (客户端ip)
 - updated_by string (客户端ip)
  


## 配置文件

定时器相关配置使用 `1m+20s` 的格式，这里表示定时1分钟，随机偏移1-20秒后再执行，偏移量是可选的

- 绑定ip端口
- 反向代理模式 
- 管理员密钥哈希(配合工具生成)
- 数据库路径
- 数据库大小限制
- 数据库大小检测周期(定时器)
- 过期清理周期(定时器)
- 单条 paste 大小限制
- multipart 容错偏移量
- id 长度限制
- id 可用字符列表 
- 随机id长度
- 随机id碰撞检测次数
- 基于id长度分配ttl，如1=1h，2-3=1d，4-8=30d 9+=365d
- 自动统计触发周期(定时器)
- 速率限制相关配置
- 日志相关配置

## 日志 

- web 请求，除去Authorization头，body只用大小代替，其他信息都记录
- 数据库操作
- 内部任务


## 辅助工具

- 生成管理员密钥哈希

